{% extends 'base_admin.html.twig' %}

{% block title %}Items{% endblock %}

{% block body %}
    <h1>Items</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ item.name }}</td>
            </tr>
            <tr>
                <th>Crud</th>
                <td>{{ item.crud }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ item.description }}</td>
            </tr>
            <tr>
                <th>Little_description</th>
                <td>{{ item.littleDescription }}</td>
            </tr>
            <tr>
                <th>Sell</th>
                <td>{{ item.sell }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ item.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('items_index') }}">back to list</a>

    <a href="{{ path('items_edit', {'id': item.id}) }}">edit</a>

    {{ include('items/_delete_form.html.twig') }}
{% endblock %}
